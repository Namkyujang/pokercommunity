# 결과 보고서 — 페르소나는 포커 의사결정을 바꾸는가

페르소나 포커 아레나에서 얻은 예비 결과 요약. 모든 수치는 아래 "재현" 명령어로
다시 만들 수 있다. 표본이 작은 예비 실험임을 분명히 밝히며, **robust한 발견과
잠정적(노이즈가 큰) 발견을 구분**한다.

---

## 1. 실험 설계

- **게임**: 1대1 노리밋 텍사스 홀덤. 양쪽 동일 스택에서 시작, 매 핸드 스택 리셋
  (토너먼트가 아니라 *행동을 고정 조건에서 비교* 하기 위함).
- **공통 신호**: 모든 에이전트는 몬테카를로로 추정한 동일한 승률(equity)을 본다.
  즉 정보는 같고, **그것을 해석하는 '태도'(페르소나)만 다르다.**
- **페르소나(규칙봇)**: 5개 파라미터(value/call 임계값, 블러프 빈도, 공격성, tilt)로 정의.
  - `bluffer` 블러퍼 · `highroller` 하이롤러 · `calm` 냉정 · `joker` 최종보스
- **LLM 페르소나**: 동일 인터페이스, 승률 숫자는 **주지 않고** 날것의 상황 + 성격 설명만 줌.
  로컬 `qwen2.5:7b`(Ollama, 무료).
- **로깅**: 모든 결정 + 이유(LLM은 chain-of-thought)를 JSONL로 기록.

지표: `raise%/fold%/call%`(행동 빈도), `bluff%`(약한 패 레이즈 = 가치 없는 공격),
`BB/100`(100핸드당 빅블라인드 순손익, 포커 표준 단위).

---

## 2. 규칙봇 baseline

### 2-1. 행동 시그니처 (200핸드 라운드로빈, seed 7)

| persona | decisions | raise% | fold% | bluff% | call% | avgRaiseBB |
|---|---|---|---|---|---|---|
| bluffer    | 905 | **75.4%** | 4.4% | **20.0%** | 20.2% | 11.5 |
| highroller | 924 | 62.0% | 7.8% | 9.0% | 30.2% | 13.3 |
| calm       | 859 | 25.5% | **33.9%** | 1.6% | 40.6% | 10.7 |

→ **행동 시그니처는 시드를 바꿔도 안정적이다.** 블러퍼는 항상 가장 자주 레이즈하고
가장 많이 블러프하며, 냉정은 항상 가장 타이트하다. ✅ **robust**

### 2-2. 수익성은 시드 변동이 크다 (각 페르소나 400핸드 × 3시드)

| persona | seed 7 | seed 11 | seed 23 | 경향 |
|---|---|---|---|---|
| **calm**   | +100.2 | +127.3 | +120.3 | **항상 흑자** ✅ |
| bluffer    | −232.9 | −198.7 | +51.4  | 대체로 큰 적자 |
| highroller | +132.7 | +71.5  | −171.7 | 극심한 변동 |

(단위 BB/100)

→ **냉정(규율)한 플레이어는 모든 시드에서 일관되게 수익을 낸다.** 반면 공격형 두 종은
변동성이 매우 커서, 시드마다 둘 중 하나가 크게 무너진다. 400핸드로는 공격형의 수익을
신뢰성 있게 추정할 수 없다 — **더 많은 핸드가 필요**. ⚠️ **잠정적**

### 2-3. 핵심: 빈도 ≠ 수익

블러퍼는 *판 수* 로는 압도적으로 많이 이긴다(예: vs calm 162–38). 블라인드를 계속
훔치기 때문이다. 하지만 그 승리는 작은 팟이고, 큰 팟에서 냉정에게 잡혀 **돈은 잃는다.**
**공격성은 빈도를 사고, 규율은 돈을 산다.**

---

## 3. LLM 페르소나 (예비, 표본 매우 작음)

로컬 `qwen2.5:7b`, bluffer vs calm, **10핸드** (소규모 파일럿).

| persona (LLM) | raise% | fold% | call% |
|---|---|---|---|
| bluffer | **100%** | 0% | 0% |
| calm    | 0% | 37% | 63% |

발견:
1. **페르소나 라벨이 베팅을 실제로 바꾼다** — 규칙봇보다도 더 극단적으로 갈림. ✅
   (동일한 약한 패 7♦2♣에서 LLM 블러퍼는 레이즈, LLM 냉정은 폴드하는 것을 단건으로도 확인.)
2. **LLM은 라벨을 인간 의도와 다르게 해석한다** — "cold/disciplined"를 "절대 공격 안 함"으로
   읽어 밸류 레이즈를 전혀 하지 않았다(raise 0%). 규칙봇 냉정(value-raise 함)과 갈리는 지점.
   → *페르소나 라벨 해석 갭* 이라는 후속 연구거리.

⚠️ N=10은 방향만 보는 수준. LLM은 비결정적이라 시드 고정도 어렵다.

---

## 4. 한계

- **표본**: 규칙봇 400핸드/시드, LLM 10핸드 — 모두 작다. 특히 LLM은 예비 수준.
- **수익 변동성**: 공격형 페르소나의 BB/100은 수천 핸드 단위가 있어야 안정 추정 가능.
- **bluff 지표 결함**: 현재 `bluff%`는 규칙봇의 equity 태그를 파싱하므로 **LLM의 블러프를
  측정하지 못한다**(표에서 LLM bluff% 생략). → 결정 시점의 실제 승률을 독립 계산해 고쳐야 함.
- **상대 적응**: 현재 페르소나는 상대를 모델링하지 않는다. "처음 보는 상대에 적응하는가"는
  미실험.

---

## 5. 다음 단계

1. bluff 지표를 실제 equity 기반으로 수정 → LLM 블러프 객관 측정.
2. 핸드 수를 늘리고 다중 시드 평균으로 수익 추정 안정화.
3. LLM 페르소나 전 종 × 다수 핸드로 행동 시그니처 정식 측정, 규칙봇 baseline과 정량 비교.
4. "처음 보는 상대 유형에 적응하는가"(opponent modeling) 실험 추가.

---

## 재현

```bash
# 2-1, 2-2 규칙봇 (시드 바꿔가며)
python3 -m arena.tournament --hands 200 --seed 7  --samples 100
python3 -m arena.tournament --hands 200 --seed 11 --samples 100
python3 -m arena.tournament --hands 200 --seed 23 --samples 100

# 3. LLM vs LLM (Ollama 서버 + qwen2.5:7b 필요)
python3 -m arena.llm_run --p0 ollama:bluffer --p1 ollama:calm --hands 10
python3 -m arena.analyze <생성된 trajectory.jsonl>
```

규칙봇은 시드 고정 시 재현됨. LLM(Ollama)은 비결정적이라 수치가 매번 다를 수 있다.
